A lexical search engine built from scratch in Python with inverted index, boolean queries, tf-idf ranking, positional/phrase queries. Currently in progress: BM25. The retrieval layer underneath modern RAG.

Run python index.py to build the index.
